# ** File generated automatically, do not modify **

SET(LIBRAW_INCLUDE_DIRECTORY @LIBRAW_INCLUDE_DIRECTORY@)
SET(LIBRAW_LIBRARIES @LIBRAW_LIBRARIES@) 
SET(LIBRAW_BUILD_PATH @LIBRAW_BUILD_PATH@)

IF(LIBRAW_INCLUDE_DIRECTORY)
	SET(LIBRAW_FOUND true)
ELSE()
	SET(LIBRAW_FOUND "NOT_FOUND")
ENDIF()
